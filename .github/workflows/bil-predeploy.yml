name: BIL pre-deploy receipt

on:
  pull_request:
  workflow_dispatch:

jobs:
  diya-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Diya gate (FAIL TEST)
        run: |
          echo "Forcing failure to test fail-closed"
          exit 1

  bil_receipt:
    needs: diya-gate
    runs-on: ubuntu-latest
    steps:
      - name: Generate BIL
        run: |
          echo "This should NOT run if Diya fails"
