name: Consumer pipeline

on:
  workflow_dispatch:

jobs:
  diya-gate:
    uses: ./.github/workflows/diya-reusable.yml
    secrets: inherit

  consumer-job:
    needs: diya-gate
    runs-on: ubuntu-latest
    steps:
      - name: Consumer step
        run: echo "Consumer pipeline runs only after Diya passes"
