# Quickstart

This guide shows the minimal path to run Provenance Gate.

## Goal

Verify an artifact, generate evidence, write a ledger entry,
and verify the ledger.

## One-command path

Run:

```bash
python3 run_pipeline.py
